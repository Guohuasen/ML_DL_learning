请用"宏观 → 逐层 → 细节"的方式讲解 <代码/主题>。

【水平说明】
\- 我见过并能看懂 Python 最基础的语法：变量、列表/字典、函数、循环、
 if、import——知道它们是干什么的，不用解释概念；
\- 但没系统学过、实际敲得少，遇到更高级的写法（列表推导式、f-string、
 lambda、装饰器、魔法方法 __init__/__getitem__、with 上下文管理器、
 解包赋值、try/except、生成器等）我不一定认识，请遇到时单独拆开讲清楚；
\- 库函数/API 讲清楚：干什么用、参数含义、返回值；
\- 函数名尽量拆解英文含义帮我记忆（如 compose=组合、squeeze=压缩、
 splitext=split+ext、DataLoader=数据装载），有规律的成对关系顺带点一下；
\- 同一个功能有多种等价写法时（如 @torch.no_grad() 装饰器 vs
 with torch.no_grad() 上下文管理器），要说明两者的区别和各自适用场景，
 别只讲一种；
\- 需要按顺序/清单记忆的知识（如"要设哪几个随机种子、在哪设"），
 不要只罗列清单，先讲清根本原因或分类依据，让清单从原理里自然推导出来；
\- 能用肉眼感受效果的内容（进度条、打印输出、张量形状变化、图像变换前后
 等），不要只文字描述：给出代码并直接展示运行后屏幕上会看到什么，
 让我亲眼看到效果差异。

【代码与讲解形式】
\- 贴出的任何代码都要带注释，小段也不例外；
\- 讲解函数时，除非函数特别长，先完整贴出带注释的整个函数让我先看整体，
 再切分逐行讲；特别长的函数才直接切分讲；
\- 易混内容用表格，抽象概念用类比；重要的设计讲"为什么";
\- 主动提醒这类任务常见的坑；
\- 结尾给一页纸总结 + 一分钟回顾。

【侧重点】我指定重点时（如"重点讲 XX""只看 YY 部分"），
其他内容一句话带过即可，不用平均用力。